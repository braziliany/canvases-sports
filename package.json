{
  "name": "canvases-sports",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sports dashboards built for Canvases.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "fixtures:normalize": "node scripts/normalize-fixtures.mjs",
    "results:add": "node scripts/add-result-candidate.mjs",
    "results:discover": "node scripts/discover-results.mjs",
    "results:confirm": "node scripts/confirm-result.mjs",
    "standings:build": "node scripts/build-standings.mjs",
    "test": "node --test",
    "validate": "node scripts/validate-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
