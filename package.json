{
  "name": "canvases-sports",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sports dashboards built for Canvases.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "fixtures:normalize": "node scripts/normalize-fixtures.mjs",
    "test": "node --test",
    "validate": "node scripts/validate-data.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
